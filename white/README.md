# 白底原版 · 独立保存

访问入口：`/white/`。深色滚屏版入口：`/scroll/`。

本目录恢复自提交 `1586cba5e83d55212161b2a7d03cf6073a8a7fda` 的完整 React 源码。
保留白色背景、圆角首屏、H3 学习场景视频、品牌轮播、培训方案和咨询功能。
无随页面滚动推进视频的效果；原有视频播放、品牌轮播和按钮动效保留。

归档源码仅调整素材相对路径，业务内容与样式不改。独立入口调整路径与浏览器标题，
并在挂载后恢复直接访问的章节锚点，支持 `/white/#contact`；原源码十个文件完整保留。
独立入口只加载本目录的样式与组件，不依赖深色版的组件，避免后续修改互相影响。
素材复用仓库的 assets 目录，不重复复制视频。

不要将 `/static/` 当作本版；该已有入口是深色版关闭动效的兼容入口，未覆盖或删除。
